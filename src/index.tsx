import * as React from "react";
import * as ReactDOM from "react-dom";

import { LinkedListManager } from "./components/LinkedListManager";

ReactDOM.render(
    <LinkedListManager />,
    document.getElementById("example")
);